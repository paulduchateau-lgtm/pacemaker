// Page Contexte mission v2 — réexport de la page v1 sous le shell v2.
// Le contenu (édition contexte LLM + thème livrables) est identique,
// seule la chrome (sidebar v2 au lieu de TopBar v1) change.
export { default } from "../../contexte/page";
